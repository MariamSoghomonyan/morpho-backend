model HomePageConsultations {
  id                String   @id @default(uuid())
  lang              String
  title             String
  descr             String
  consultation_btn  String
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}